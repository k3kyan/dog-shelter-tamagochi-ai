import httpx, pytest
from fastapi.testclient import TestClient

# test by running this in terminal:
# pytest tests/ -v
# TODO: actually i will do tests later bc i already tested with swaggerUI and docker dynamodblocal and everything worked